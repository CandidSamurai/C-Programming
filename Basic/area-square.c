// AREA OF A SQUARE

#include <stdio.h>

int main()
{
    int s;
    printf("Enter the side of the square:");
    scanf("%d", & s);
    int area= s*s;
    printf("Area of the square= %d",area);
    return 0;
}